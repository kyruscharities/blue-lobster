%h2= @question.question

= simple_form_for @answer do |f|
  = f.input :score, collection: [1, 2, 3, 4, 5], as: :radio_buttons, default: 1
  = f.hidden_field :question_id, value: @question.id
  = f.submit "Answer"
