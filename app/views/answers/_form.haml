= simple_form_for [@question, @answer] do |f|
  = f.error_notification

  = f.input :score, collection: [1, 2, 3, 4, 5], as: :radio_buttons
  = f.association :question
  = f.association :user, label_method: :email
  = f.submit 'Answer'
